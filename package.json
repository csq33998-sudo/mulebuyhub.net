{
  "name": "mulebuyhub-net",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "clean:catalog": "node scripts/clean-catalog-data.mjs"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10"
  }
}
